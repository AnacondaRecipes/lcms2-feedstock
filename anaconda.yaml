anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: mm2/Little-CMS
      use_max_tag: true
      include_regex: (?i)^lcms2\.\d+$
      from_pattern: (?i)^lcms2\.(\d+)$
      to_pattern: 2.\1
